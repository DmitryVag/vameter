Source code. The code that should be run un computer is in the "server" directory,
the code for Arduino board is in the "client" one (logically it should be the 
opposite, but I understood that later). 
